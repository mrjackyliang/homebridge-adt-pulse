import {PlatformAccessory} from 'homebridge';

import { ADTPulsePlatform } from '@/lib/platform';

/**
 * ADT Pulse Platform Accessory.
 *
 * @since 1.0.0
 */
export class ADTPulsePlatformAccessory {
  constructor(platform: ADTPulsePlatform, accessory: PlatformAccessory) {
  }
}
